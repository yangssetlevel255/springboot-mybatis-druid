<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dhu.love.project.mapper.DemoMapper">

    <!-- 查询你的爱 -->
    <select id="searchLove" resultMap="java.lang.Math" parameterType="String">
        select * from userinfo where info_id=#{param}
    </select>

</mapper>